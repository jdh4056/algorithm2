#include<bits/stdc++.h>
using namespace std;

int main() {
    int n; cin >> n;
    while (n--) {
        int x,y; cin>>x>>y;

        if (x >= y) cout << "MMM BRAINS\n";
        else cout << "NO BRAINS\n";
    }
}