#include <bits/stdc++.h>
using namespace std;

int main() {
    int T,F,S,P,C;
    int t,f,s,p,c;
    cin >> T >> F >> S >> P >> C;
    cin >> t >> f >> s >> p >> c;
    cout << (T*6) + (F*3) + (S*2) + (P) + (C*2) << '\n';
    cout << (t*6) + (f*3) + (s*2) + (p) + (c*2) << '\n';
}