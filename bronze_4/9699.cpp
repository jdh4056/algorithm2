#include<bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
#include<bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t;
    cin>>t;
    for(int i=0;i<t;i++) {
        int n[5];
        for(int i=0;i<5;i++) cin >> n[i];
        int mx = 0;
        for(int i=0;i<5;i++) {
            if(n[i]>mx) mx = n[i];
        }
        cout << "Case #" << i+1 << ": " << mx << '\n';
    }
}
    int t;
    cin>>t;
    for(int i=0;i<t;i++) {
        int n[5];
        for(int i=0;i<5;i++) cin >> n[i];
        int mx = 0;
        for(int i=0;i<5;i++) {
            if(n[i]>mx) mx = n[i];
        }
        cout << "Case #" << i+1 << ": " << mx << '\n';
    }
}