#include <bits/stdc++.h>
using namespace std;

int main() {
    int z;
    cin >> z;
    while (z--) {
        int w,k;
        cin >> w >> k;

        int traveler = (w * k) / 2;
        cout << traveler << '\n';
    }
}